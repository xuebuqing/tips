
github用户名
xuebuqing

github密码
github管理
git branch -a
git checkout -b feature origin/feature
git branch
git checkout feature
git remote -v

172.21.84.20
111111

git rebase -i HEAD~2
 
squash 的意思是这个 commit 会被合并到前一个commit(或者s)

pwd 查看linux目录

putty 

mtputty

filezilla client 查看文件

chmod -R 777 *

scp: /home/xue/backlight: Permission denied   不能复制到home 没有权限
查看进程
ps -ef | grep ssh

find . -name -print | grep ~
翻墙
ssh -C -f -N -q -D 8880 user@172.16.12.168
密码：user

0 ：它是数字0。可以快速移动光标到所在行的行首。

$ ：英文输入下，按 shift 和数字键 4 即可。可以快速移动光标到所在行行尾第一个非空字符下。

^ ：英文输入下，按 shift 和数字键 6 即可。可以快速移动光标到所在行，「第一个非空字符」处。因为敲代码时总有缩进，当然希望到这一行代码的第一个字母，而不是这一行行首的空格了。

go语言查看结构图
查看主函数
go-callvis -minlen 4 /home/advantech/modbus-engine
go-callvis -skipbrowser -nostd ./
查看指定包
cd /home/advantech/modbus-engine
go-callvis -group pkg,type -focus github.com/edgehook/modbus-engine/pkg/mqtt  /home/advantech/modbus-engine

ubuntu系统

建立软连接
ln -s /home/advantech/gopath /usr/bin/

echo $PATH

查看当前目录
pwd
复制
sudo cp /usr/share/zoneinfo/Asia/Japan /etc/localtime

按下Esc进入控制模式

u 表示撤销上一步命令；

Ctr+r 表示恢复上一步被撤销的命令；

大量移动文件    sudo nautilus
删除文件夹  sudo rm -rf 文件
终端创建文件夹  mkdir 文件名
返回上一级目录  cd ..
显示当前目录    pwd
复制        cp mm /
创建工作空间    mkdir catkin_ws
显示目录文件    ls
创建包      catkin_create_pkg 包名 roscpp rospy std_msgs
编译包      1. cd catkin_ws 2. catkin_make
运行        1. roscore  2. source devel/setup.bash  3. rosrun 包名 可执行文件名
自动补全    Tap
打开终端    ctrl+alt+t
同终端打终端    ctrl+shift+t
终端复制粘贴    ctrl+shift+C/V
切换任务栏  alt+Tap
结束终端任务    ctrl+c
退出终端    ctrl+d
系统重启    sudo reboot
截图        shift+prtsc（打印键）
打开文件    code file
查看本机ip  ifconfig -a
快速删除已输入  ctrl+c
清空终端    ctrl+l
超声波USB权限   sudo chmod 777  /dev/ttyUSB0
编译c++文件     g++ -o test main.cpp /usr/lib/libcontrolcan.so  -lpthread
更新source      source ~/.bashrc
conda create -n env_name numpy matplotlib python=3.7
使用命令conda remove -n your_env_name(虚拟环境名称) --all
使用命令conda remove --name $your_env_name  $package_name（包名） 即可。
安装window软件 wine+软件
配置文件到指定目录下 /.config prefix=
安全复制 scp -r 文件 用户名@ip：~
安装安装包 sudo dpkg -i
sudo apt -get install -f -y
光标选中 点击中间即可复制
安装搜狗输入法后，还需要配置当前输入法


top 查看內存
ifconfig 查看ip

在vim 中的快捷命令：
dd  删除一行
x   删除单个字符
：q!    退出
：qw    不保存退出
gg  跳转到首行
shift + g   跳转到最后一行

/word   命令模式下搜索word
然后按n查找下一个匹配的单词
按N查找上一个匹配的单词

终端快捷命令
Ctrl + A 到终端首
Ctrl + E 到终端尾
Ctrl + U 擦除到行首
Ctrl +pageup    同终端切换
Ctrl +pagedown  同终端切换

watch -n 10 nvidia-smi 显卡gpu
source activate
